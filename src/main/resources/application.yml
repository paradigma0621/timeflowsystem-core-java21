server:
  port: 0 # intentionally 0 to use a random free port

spring:
  application:
    name: core-java21
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    jdbc-url: jdbc:mysql://localhost:3306/mysqlDatabase
    username: timeflow
    password: 12345678

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}